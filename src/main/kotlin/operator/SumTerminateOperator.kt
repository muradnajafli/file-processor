package operator

/**
 * Class that takes Int and produces Int.
 */
class SumTerminateOperator : TerminateOperator {
    /**
     * Method that terminates input sequence and produces int value equal to the sum of each sequence item.
     * @param param input sequence to terminate
     * @return sum of each sequence item
     */
    override fun terminate(param: ) = TODO()
}